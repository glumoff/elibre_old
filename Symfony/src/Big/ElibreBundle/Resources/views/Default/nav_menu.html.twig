{% for cur_theme in themes %}
<h3>{{ cur_theme.title }}</h3>
    {% if cur_theme.children %}
      <div>
        {% include "BigElibreBundle:Default:nav_menu.html.twig" with {'themes': cur_theme.children.getThemesArray()}  %}
      </div>
    {% endif %}
{% endfor %}
